chore(workspace): apply audit fixes from external review

Round 1 (P0 critical):
1. ADR-003: chot HealthGuard trunk = develop (deploy = user-owned release branch)
2. Replace deploy refs in workflows (topology, start, debug, fix-issue, review)
3. Upgrade hooks:
   - protect_secrets.py: add content scan for hardcoded API key/JWT/password/AWS/Firebase/GitHub/Stripe tokens + PEM blocks
   - block_dangerous_commands.py: add branch guard (block git commit/push on trunk),
     destructive DB guards (Prisma reset, DROP TABLE, TRUNCATE),
     PowerShell Remove-Item -Recurse, docker-compose down -v, git clean -fdx
4. Convert rules trigger types per Windsurf docs:
   - always_on (3): 00-operating-mode, 40-security-guardrails, 60-context-continuity
   - model_decision (5): 10, 11, 20, 30, 50 (with description for AI to load when relevant)
   - glob (5): overlay rules 21-25 (Flutter, FastAPI, Express+Prisma, React+Vite, docs+SQL)

Round 2 (P1 high):
5. Update README: counts (8 rules, 22 skills, 14+1 workflows) + categorized slash commands + skill grouping + rule activation modes section
6. Run sync.ps1 -Mirror: remove stale skills (flutter-firebase-patterns, nodejs-ts-backend) from 5 deployed copies + clean old mobile-agent references
7. Replace cd with cwd notation per Cascade tool policy in 5 workflows (build, debug, deploy, review, cross-repo-feature)
8. Replace Meep/feed/PostRepository examples with VSmartwatch (SOS, fall, vital, device) in 6 skills:
   karpathy-guidelines, writing-plans, tdd, systematic-debugging, verification-before-completion, code-review-five-axis, caveman-vi
9. Add /close-task workflow: post-merge cleanup (verify merge, pull trunk, delete branches, update bug log/ADR/JIRA, optional tag)

Round 3 (P2 polish):
10. Soften rule 10 UC requirement: domain bugs require UC, infra/tooling bugs do not
11. Fix rule 00 infra guard paths: explicit infra-only list + context-dependent (scripts/, docs/, migrations/) qualifiers

Khong fix (push back per operating mode):
- Skill tier per-repo (P1-8): progressive disclosure already minimizes bloat, tiering adds maintenance cost
- new-bug.ps1/new-adr.ps1 automation (P1-7): YAGNI with 0 bugs + 2 ADRs current
- /deploy modification (P2-11): deploy is user-owned, AI khong touch
- 60-context-continuity to model_decision: defeat anti-loop purpose
